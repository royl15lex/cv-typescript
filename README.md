# cv-typescript
Simple cv-typescript example

## Features
- A simple example demonstrating TypeScript capabilities for building a CV.

## Installation
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Run `npm start` to see the example in action.

## Future Enhancements
- Add more examples and improve the styling of the CV.